<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nice.quickpizzaclint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuickPizzaClint">
        <activity android:name=".Views.Updatafood"></activity>
        <activity android:name=".Views.Test" />
        <activity android:name=".Views.Cart" />
        <activity android:name=".Views.Food" />
        <activity
            android:name=".Views.Catogrey"
            android:label="@string/title_activity_catogrey"
            android:theme="@style/Theme.QuickPizzaClint.NoActionBar" />
        <activity android:name=".Views.Register" />
        <activity android:name=".Views.Signin" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>